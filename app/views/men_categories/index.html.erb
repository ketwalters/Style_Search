<% @categories.each do |category| %>
  <% if category.type == "M_Formal" %>
  <%= link_to "#{category.type}", mformalitems_path %>
  <% elsif %>
  <% if category.type == "M_Work" %>
  <%= link_to "#{category.type}", mworkitems_path %>
  <% else %>
  <% if category.type == "M_Casual" %>
  <%= link_to "#{category.type}", mcasualitems_path %>
  <% end %>
  <% end %>
  	
  <% end %>
  
    <% end %>